# XAI01-04 归因方法模式发现 —— 小组工作指导文件

> 适用组:本项目(XAI01-04)小组(5人) · 更新时间:2026-09-04
> 用途:全组统一的角色分工、技术方案、实验设计与协作规范。所有决策以本文件为基准,变更须在例会中同步并更新本文件。

---

## 0. 任务一句话定位

**我们不提出新方法。** 我们做的是:把多类标准归因方法(梯度类/扰动类/博弈论类)放进一个
"方法 × 模型 × 数据集 × 指标"的矩阵里系统比较,用统计方法(ANOVA/Pareto/相关性)发现规律,
最终回答"什么条件下用什么方法最优",产出一份**方法选择指南**。

核心挑战(评分看点)有两处:**实验设计的完备性**(矩阵怎么搭、组合爆炸怎么管)和
**模式发现的统计显著性**(p 值、效应量,不能只看均值排名)。

---

## 1. 任务要求速览(摘自任务书 XAI01-04)

### 1.1 研究问题

| 编号 | 问题 |
|------|------|
| RQ1 | 在"方法×模型×数据集×指标"空间中,是否存在稳定的优势模式——某种方法在某种条件下系统性优于其他方法? |
| RQ2 | 不同归因方法在忠实性、稳定性、效率三个维度上的权衡关系如何——是否存在 Pareto 最优方法? |
| RQ3 | 不同评价指标之间是否存在系统性冲突——高忠实性的方法是否必然低稳定性? |

### 1.2 必做实验

1. **方法×模型交叉对比**(→RQ1):固定数据集和指标,所有方法在 ≥2 种模型上对比,检验交互效应。
2. **Pareto 前沿分析**(→RQ2):在忠实性/稳定性/效率三维空间画各方法 Pareto 前沿。
3. **指标冲突分析**(→RQ3):指标间相关性矩阵,找出冲突最严重的指标对。
4. **模式统计检验**:ANOVA 检验方法/模型/数据集的主效应与交互效应显著性,报告 p 值和效应大小。

### 1.3 选做实验(至多选 2 项,建议选:方法选择决策树 + 方法聚类)

- 实验5:前沿方法(FourierShap / MA-GIG)加入比较
- 实验6:方法聚类(指标空间中发现方法群体)
- 实验7:方法选择决策树(可操作的选择指南)

### 1.4 里程碑(M1–M5,与任务书对齐)

| 里程碑 | 内容 | 关键交付物 |
|--------|------|-----------|
| M1 问题分析 | 实验矩阵设计、组合爆炸管理策略、RQ1–RQ3 拆解 | 矩阵设计文档 |
| M2 初步结果 | 2 种基线方法运行、分析 pipeline 建立 | 可跑通的 pipeline |
| M3 中期结果 | ≥4 种方法运行、初步对比和 Pareto 分析 | 中期检查演示材料 |
| M4 完整实验 | 完整矩阵、ANOVA 和相关性分析、选做实验 | 全量结果 + 统计结论 |
| M5 最终成果 | 报告(Pareto 分析、RQ1–RQ3 回答、方法选择指南)、可复现代码仓库、答辩 PPT | 三个终稿 |

### 1.5 任务书建议的实验空间(我们在此基础上定稿)

- **方法(≥4)**:Grad-CAM / IG / SHAP / RISE / LIME / Ablation(选做:Saliency 作为低成本第 7 种对照)
- **模型(≥2)**:VGG-16 / ResNet-50 / DenseNet-121
- **数据集(≥2)**:ImageNet / VOC / CHNCXR
- **指标(≥3)**:忠实性 / 稳定性 / 效率

---

## 2. 角色分工总览

分工原则:**一人一个 Owner(负责人),但人人写代码**。Owner 对交付物负责,不等同于唯一执行人——
工作量随时间转移(前期数据/模型重,中期实验重,后期分析/文档重),各阶段允许互相支援。

| 角色 | 核心职责(一句话) | 关键交付物 | 兼任 |
|------|------------------|-----------|------|
| 数据负责人 | 数据集获取、评估集采样、预处理 pipeline | `data/` + `preprocessing/`、评估集清单 | — |
| 模型负责人 | 3 个预训练模型的加载封装与推理统一接口 | `models/`、模型准确率表 | — |
| 实验负责人 | 归因方法实现、评价指标、实验运行与结果落盘 | `experiments/`、`results/` 全量 CSV | 环境/GPU 维护 |
| 分析负责人 | 统计检验、Pareto、相关性、聚类/决策树 | `analysis/` 脚本与图表、方法选择指南 | 帮实验跑批 |
| 文档负责人 | 报告、README、PPT、会议纪要、里程碑材料 | `docs/`、M5 三个终稿 | **项目管理(组长)** |

项目管理由文档负责人兼任(若组内已有组长,则组长承担):排期、每周例会、任务看板、风险跟踪、
与指导教师沟通。**组长的权力是"催进度和做决定",不是包办。**

---

## 3. 各角色详细职责

### 3.1 数据负责人

**M1 阶段就要动起来的事(本周):**

1. **下载课程网盘资源**(链接见下,注意 **2026-12-11 过期**,尽早下载并双备份):
   - 链接:`https://pan.tju.edu.cn/link/AAC85903DEA75D4DC3B8E604FED612552E`
   - FolderName:`resources` · 提取码:`oc7B`
   - 重点看 CHNCXR(胸部 X 光)的目录结构、图像数量、是否有标签/bbox 文件。
2. **确认 ImageNet 获取途径**:ImageNet 需注册下载,先与指导教师确认校内是否有现成资源;
   若拿不到,备选方案见 §9 风险 2,但**此决策必须在 M1 内完成**,不能拖。
3. VOC:使用 VOC2007 test 或 VOC2012 val,公开易下载,且自带 bbox(可用于选做的定位性指标)。

**常规职责:**

- 建立并维护 `data/` 目录结构与 README(每个数据集:来源、规模、许可、版本、放置路径)。
- 制定**评估集采样方案**:每个数据集抽取固定评估集(建议 300–500 张,按类别分层采样;
  另建 30–50 张的 debug 集给开发期快速调试)。采样**只做一次**,全组共用同一评估集,保证可比。
- 产出 `preprocessing/`:`load_image → resize(统一 224×224)→ 归一化(ImageNet 均值方差)` 的统一数据入口,供模型和实验直接调用。
- 生成 `metadata.csv`(每张图一行):`image_id, dataset, class_label, split`。
- 各数据集的类别映射表(ImageNet 1000 类索引 vs VOC 20 类 vs CHNCXR 的类别,统一成组内约定格式)。

**验收标准**:任何组员按 `data/README.md` 说明,在 10 分钟内能把 3 个数据集的评估集加载出来。

### 3.2 模型负责人

- 用 `torchvision.models` 加载 3 个 ImageNet 预训练模型:`vgg16` / `resnet50` / `densenet121`
  (`weights=IMAGENET1K_V1`),封装统一推理接口 `predict(model, image) -> (pred_class, conf, logits)`。
- 对每个数据集统计 3 个模型的准确率(直接推理,不调参),产出"模型×数据集准确率表"——
  这张表是后续分析的重要**条件变量**(模型在数据上表现好不好,本身就是模式的一部分)。
- **M2 决策点**:若 CHNCXR 上预训练权重准确率过低(与随机无异),启动微调(少量 epoch,冻结 backbone 微调分类头即可,
  务必记录微调配置);若可用则直接零样本推理并在报告中说明。
- 记录每张评估图的逐样本预测结果(`image_id, model, pred_class, conf, is_correct`),合并进 metadata,
  用于后续"正确/错误分类样本"的子条件分析(探索项,不放进主矩阵)。
- `models/` 目录保存:加载脚本、微调代码(若有)、checkpoint(或下载命令)。

**验收标准**:实验负责人可以 `from models import load_model` 拿到任意模型并完成一次前向。

### 3.3 实验负责人(中期最重,全组支援)

- 统一归因接口:`get_attribution(method, model, image, target) -> np.ndarray(H,W)`,
  6 种主体方法全部走这一个接口(梯度类:IG、Grad-CAM;扰动类:RISE、LIME、Occlusion/Ablation;
  博弈论类:KernelSHAP;另加 Saliency 做低成本对照)。
- 方法实现优先用现成库,不重复造轮子:Captum(IG/Saliency/Occlusion/KernelSHAP)、pytorch-grad-cam(Grad-CAM)、
  lime 包(LIME)、RISE 手写约 30 行(随机掩码平均)。
- 评价指标(≥3 维,每维可多指标):
  - **忠实性**:像素扰动 AUC(MoRF/LeRF,按归因排序逐步扰动、看预测概率下降曲线下面积),可用 Quantus 的
    PixelFlipping / FaithfulnessEstimate;补充 Comprehensiveness/Sufficiency。
  - **稳定性**:Max-Sensitivity(输入加噪后归因变化量,Quantus 现成)。
  - **效率**:固定机器上每种方法每图平均耗时(wall-clock,排除 IO 与预热,取 50 图均值)。
  - (选做加分)定位性:VOC 有 bbox,可做 Pointing Game;合理性:模型参数随机化 sanity check。
- 写配置驱动的运行脚本 `experiments/run_unit.py`,YAML 定义矩阵,**一次运行一个(方法,模型,数据集)单元**,
  支持断点续跑,结果落盘为统一 CSV(见 §7.3 schema)。
- 组合爆炸管理(直接关系任务书加分项):54 个主体单元 = 6 方法 × 3 模型 × 3 数据集,靠
  ① 统一评估集 + ② 配置驱动 + ③ 断点续跑 + ④ 小集调试→大集正式 两阶段来管,详见 §5。
- 维护 `requirements.txt` 锁版本、实验运行日志(每单元的配置快照+耗时)。

**验收标准**:`python experiments/run_unit.py --config configs/unit_resnet50_imagenet.yaml` 一条命令跑完一个单元并产出 CSV。

### 3.4 分析负责人

- 把 `results/` 的逐图 CSV 聚合成**长表**(unit-level:每行 = 方法×模型×数据集×指标×均值/标准差),统一入 `analysis/data_long.csv`。
- 实现并跑通统计脚本(`analysis/`):
  - **ANOVA**(三因素:method × model × dataset,逐指标做):报告 F 值、p 值、**效应量(partial η²)**;
    若正态性/方差齐性不满足,用非参数 Friedman + Nemenyi 事后检验 + 临界差异(CD)图。
  - 主效应图 + **交互效应图**(RQ1 的直接证据)。
  - **Pareto 前沿**(RQ2):三维空间 忠实性↑ × 稳定性↑(Max-Sensitivity 取负)× 效率↑,
    画前沿、标出 Pareto 最优单元。
  - **相关性矩阵**(RQ3):指标对 Pearson/Spearman,找出冲突最严重的指标对。
  - 选做:层次聚类(方法行为分组)、决策树(特征:model/dataset/准确率 → 标签:该单元最优方法,树深 ≤3 保证可读)。
- 汇总成**方法选择指南**(模板见附录 C),这是 M5 的核心成果。
- M3–M4 阶段实验负责人跑批压力大时,分析负责人是**第一支援人**(先拿到部分数据就可以开始写分析脚本)。

**验收标准**:任何一张图、一个统计结论都能指出"由 `analysis/xxx.py` 从 `data_long.csv` 复现"。

### 3.5 文档负责人(兼项目管理)

- **项目管理**:排期(见 §6)、每周例会主持与纪要(`docs/meetings/`)、任务看板(issue 或表格)、风险跟踪。
- **文档**:最终报告(结构见 §7.4)、答辩 PPT、README、各里程碑的提交材料整合。
- 文档不是最后才写:从 M1 起维护报告草稿大纲,每个里程碑往里面填一章,避免期末突击。
- 统一组内术语(中文/英文对照:忠实性-faithfulness、稳定性-stability、效率-efficiency 等),
  统一图表风格(配色、字体、图注格式),报告与 PPT 共用同一套图。

**验收标准**:任意时刻,报告草稿都能反映当前进度;任何组员不知道自己该干什么时,看板里有答案。

---

## 4. 技术方案(全组统一)

### 4.1 环境与技术栈

- Python 3.10+,PyTorch(建议 2.x,CUDA 版),torchvision
- 归因:`captum`(IG/Saliency/Occlusion/KernelSHAP/DeepLIFT)、`pytorch-grad-cam`、`lime`
- 评价:`quantus`(忠实性/稳定性指标,注意锁版本,API 变动较多)
- 统计:`pandas` / `numpy` / `scipy` / `statsmodels`(ANOVA)/ `scikit-learn`(聚类、决策树)、
  可选 `scikit-posthocs`(Nemenyi)、`autorank`(CD 图)
- 可视化:`matplotlib` / `seaborn`
- 配置:`pyyaml`;进度条:`tqdm`

```bash
# 环境初始化(conda 或 venv 均可,由实验负责人统一)
conda create -n xai01-04 python=3.10
conda activate xai01-04
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
pip install captum quantus pytorch-grad-cam lime pandas scipy statsmodels scikit-learn matplotlib seaborn pyyaml tqdm
pip freeze > requirements.txt   # 锁版本后全组同步
```

### 4.2 归因方法集(主体 6 + 对照 1)

| 类别 | 方法 | 实现途径 | 说明 |
|------|------|---------|------|
| 梯度类 | Integrated Gradients (IG) | captum | 基线统一用零基线(组内一致即可) |
| 梯度类 | Grad-CAM | pytorch-grad-cam | 注意用 VGG 时目标层选最后一个 conv |
| 梯度类 | Saliency(对照) | captum | 原始梯度,实现成本≈0,用作组内 sanity |
| 扰动类 | RISE | 手写 ~30 行 | 随机掩码平均,N 掩码数记入配置 |
| 扰动类 | LIME | lime 包 | 超像素分割用 quickshift,记入配置 |
| 扰动类 | Occlusion/Ablation | captum | 窗口大小记入配置 |
| 博弈论类 | KernelSHAP | captum | 若太慢可用 DeepSHAP 替代并注明 |

选做(FourierShap / MA-GIG):只有主体 6 种全部跑完且有富余人力时才启动,不影响主线。

**组内约定**:所有方法归因目标统一为**真值类别**(target = ground-truth class),而非预测类别;
模型负责人额外记录预测类别,方便后续"预测正确/错误"子条件分析。

### 4.3 数据集与模型矩阵

| 维度 | 选项 | 备注 |
|------|------|------|
| 数据集 | ImageNet(val 采样)、VOC(2007 test 或 2012 val)、CHNCXR | 每集取固定评估集 300–500 张分层采样 |
| 模型 | VGG16 / ResNet50 / DenseNet121 | torchvision 预训练,统一 224×224 输入 |

---

## 5. 实验矩阵与组合爆炸管理(任务书核心加分点)

### 5.1 矩阵

**主体单元 = 6 方法 × 3 模型 × 3 数据集 = 54 个单元**,每个单元对固定评估集全量计算
忠实性/稳定性/效率全部指标(指标不算乘法项)。逐图结果 + 单元聚合两层保存。

### 5.2 组合爆炸管理策略(写入 M1 文档)

1. **统一评估集**:采样一次,54 个单元共用 → 保证可比,也保证总量可控。
2. **配置驱动 + 断点续跑**:每个单元一个 YAML,中断后从已完成 image_id 续跑。
3. **两阶段**:
   - 开发期(M2):debug 集 30–50 张,快速打通 pipeline、检查各方法报错和指标合理性;
   - 正式期(M3–M4):全评估集,GPU 空闲时间批量跑,记录每个单元耗时。
4. **预算估算**(写进 M1 文档):扰动类指标每图需 ~50 次前向,单 GPU 前向约 30ms →
   500 图 × 50 步 × 30ms ≈ 12 分钟/单元,54 单元 ≈ 11 小时量级,可接受;
   KernelSHAP/LIME 最慢,必要时限制其评估集子集(如 150 张)并在报告中注明(效率本身就是发现点,不要隐瞒)。
5. **不扩维**:主矩阵固定 3 个维度。"预测正确/错误""模型准确率"等条件作为**事后分组变量**分析,不放进主矩阵。

### 5.3 结果统一 schema(§7.3 细述)

逐图:`image_id, dataset, model, method, metric, value, time_ms`
单元:`method, model, dataset, metric, mean, std, n, config_hash`

---

## 6. 里程碑计划(周历)

> 按 2026-09-04(第 1 周)起算;**以指导教师公布的 deadline 为准调整**,但"M3 前必须有完整小规模端到端结果"是硬底线。

| 周次(日期) | 里程碑 | 各角色重点 | 组会交付 |
|-----------|--------|-----------|---------|
| W1 (09-04~09-11) | **M1 问题分析** | 数据:网盘下载、数据集盘点、ImageNet 途径确认;模型:环境搭建、模型加载验证;实验:矩阵 YAML 骨架;分析:RQ 拆解与统计方案;文档:排期、README 骨架、例会制度 | 实验矩阵定稿文档、评估集方案、风险清单 |
| W2 (09-12~09-18) | **M2 初步结果** | 模型:3 模型准确率表 + CHNCXR 微调决策;实验:**Grad-CAM + SHAP 两方法跑通 debug 集**,pipeline 端到端(含 1 个忠实性指标) | 首批逐图 CSV、pipeline 演示 |
| W3 (09-19~09-25) | M2→M3 | 实验:其余 4 方法实现并在 debug 集调通;数据/模型/分析支援 | 全方法 debug 集结果汇总表 |
| W4 (09-26~10-02) | **M3 中期结果** | 实验:≥4 方法正式集开跑;分析:初步对比图 + Pareto 初版;文档:中期演示材料 | 中期检查演示材料(PPT 初稿) |
| W5–W6 (10-03~10-16) | **M4 完整实验** | 实验:54 单元全部跑完;分析:ANOVA、相关性、效应量;文档:更新报告 | 全量长表 + 统计检验初版结论 |
| W7 (10-17~10-23) | M4 收尾 | 分析:选做(决策树 + 聚类);实验:补漏单元/复现抽查;数据/模型:消缺支援 | 方法选择指南初稿 |
| W8–W9 (10-24~11-06) | **M5 最终成果** | 文档:报告终稿、PPT、README;实验:按 README 从头复现自测;全组:交叉校对 | 报告/PPT/代码仓库三件套 |
| W10 (11-07~) | 缓冲 + 答辩演练 | 全组 | 答辩演练、常见问题清单 |

**例会节奏**:每周 1 次固定例会(30–60 分钟,文档负责人主持,纪要入 `docs/meetings/`),
会前每人 3 句话(做了什么/卡在哪/下周做什么);平时群聊异步同步,进度看板(共享表格)随时可查。

---

## 7. 协作与工程规范

### 7.1 仓库目录(对齐任务书"代码仓库要求")

```
.
├── data/             # 数据集原始文件 + README(数据负责人)
├── preprocessing/    # 数据加载、评估集采样、metadata 生成(数据负责人)
├── models/           # 模型加载封装、微调代码、checkpoint(模型负责人)
├── experiments/      # 归因方法、评价指标、run_unit.py、configs/(实验负责人)
├── results/          # 逐图 CSV + 单元聚合 CSV + 运行日志(实验负责人)
├── analysis/         # 统计脚本、图表、方法选择指南(分析负责人)
├── docs/             # 报告、PPT、会议纪要、里程碑材料(文档负责人)
├── requirements.txt
└── README.md         # 环境配置 + 运行说明(文档负责人)
```

### 7.2 Git 规范

- 一条硬规则:**`results/` 与 `data/` 不进 git 仓库**(大文件放网盘/共享盘,git 里只放生成它们的脚本与 schema 说明)。
- 每人一个分支按模块提交,PR 由对应 Owner review;`main` 分支保持可跑。
- commit 信息写清楚"做了什么",中文即可。每个里程碑打 tag(`m1`、`m2`…)。

### 7.3 结果文件 schema(全组对齐,改了先开例会)

`results/per_image.csv`:

| 列 | 示例 |
|----|------|
| image_id | `n01440764_ILSVRC2012_val_00004102.JPEG` |
| dataset | `imagenet` |
| model | `resnet50` |
| method | `ig` |
| metric | `faithfulness_morf_auc` |
| value | `0.1823` |
| time_ms | `243.1`(仅 efficiency 类指标填) |

`results/units.csv`:`method, model, dataset, metric, mean, std, n, config_hash`(config_hash 指向 YAML 快照)。

### 7.4 最终报告结构(建议,对齐任务书要求)

1. 问题分析(背景、RQ1–RQ3 拆解)
2. 方法设计(实验矩阵、方法/模型/数据集/指标说明、组合爆炸管理策略)
3. 实验结果(方法×模型交叉对比、Pareto 前沿、指标冲突)
4. 统计检验(ANOVA、效应量、交互效应;RQ1–RQ3 的明确回答)
5. 方法选择指南(决策树/规则表,见附录 C)
6. 错误分析与局限(失败的实验、指标局限、计算资源约束)
7. 结论与改进方向

### 7.5 学术规范

- 各方法与指标必须引用原始论文(IG: Sundararajan et al. 2017;SHAP: Lundberg & Lee 2017;
  RISE: Petsiuk et al. 2018;LIME: Ribeiro et al. 2016;Grad-CAM: Selvaraju et al. 2017;等)。
- 若同班其他组做 XAI01-01~03,可以交流方法与踩坑经验,但**代码与报告必须独立完成**。
- 所有结论必须附 p 值/效应量,报告里"显著"一词要有统计依据。

---

## 8. 风险与应对

| # | 风险 | 应对 |
|---|------|------|
| 1 | 课程网盘链接 2026-12-11 过期 | **W1 内下载**完毕,本地 + 网盘双备份 |
| 2 | ImageNet 无法获取 | W1 内问清指导教师/校内资源;备选:ImageNet 子集(报告说明)或降为 VOC+CHNCXR 两数据集(仍满足 ≥2,但矩阵规模变小,需评估对 ANOVA 的影响) |
| 3 | 算力不足/训练慢 | 评估集 500→300 可缩;扰动步数 50→30;KernelSHAP 限子集;实验尽量夜间批量跑 |
| 4 | Quantus/captum API 与文档不符 | requirements.txt 锁版本;首次运行先在 debug 集验证每个指标数值范围是否合理(如 AUC∈[0,1]) |
| 5 | CHNCXR 上预训练模型准确率过低 | M2 决策点:微调分类头(小成本);报告如实呈现"低性能模型上的归因行为"这一条件 |
| 6 | 某组员进度落后 | 看板周更 + 例会点名;Owner 职责不清的任务,组长直接分派;M4 起全组支援实验/文档 |
| 7 | 结论不显著(没有"稳定优势模式") | 这本身就是合法发现——如实报告"在哪些条件下无差异",并给出效应量;决策树/指南退化为"不推荐某方法的条件",同样有价值 |

---

## 9. 交付物检查清单(对照任务书)

**M1**:实验矩阵设计文档、组合爆炸管理策略、RQ1–RQ3 拆解 ✓
**M2**:2 种基线方法运行、分析 pipeline 建立、数据预处理代码 ✓
**M3**:≥4 种方法运行、初步对比和 Pareto 分析、中期检查演示材料 ✓
**M4**:完整实验矩阵、ANOVA 和相关性分析、选做实验结果 ✓
**M5**:完整报告(模式发现 + Pareto 分析 + RQ1–RQ3 回答 + 方法选择指南)、可复现代码仓库、答辩 PPT ✓
**代码仓库**:目录结构 `data/ preprocessing/ models/ experiments/ results/` ✓
关键实验命令行可复现 ✓ README 含环境配置与运行说明 ✓

---

## 附录 A:本周(W1)行动清单

- [ ] 全组确认分工与兼任安排,把名字填进 §2 表格
- [ ] 数据负责人:下载网盘资源、盘点 CHNCXR、确认 ImageNet 获取途径
- [ ] 模型负责人 + 实验负责人:环境搭建,跑通一个预训练模型前向
- [ ] 实验负责人:搭建仓库骨架、定义 YAML 与 CSV schema
- [ ] 分析负责人:把 RQ1–RQ3 拆成可检验的统计假设(每个 RQ 对应什么检验)
- [ ] 文档负责人:例会时间确定、看板建立、README 骨架
- [ ] 全组:第 1 次例会过一遍本文件,有异议当场改

## 附录 B:常用命令示例

```bash
# 跑一个实验单元(实验负责人)
python experiments/run_unit.py --config configs/ig_resnet50_imagenet.yaml

# 聚合逐图结果(分析负责人)
python analysis/aggregate.py --input results/per_image.csv --output analysis/data_long.csv

# 三因素 ANOVA(分析负责人)
python analysis/anova.py --metric faithfulness_morf_auc

# Pareto 前沿图(分析负责人)
python analysis/pareto.py --dims faithfulness,stability,efficiency
```

## 附录 C:方法选择指南模板(M5 成果样式)

| 条件 | 推荐方法 | 依据(证据) |
|------|---------|-----------|
| 大模型(ResNet50)+ 自然图像 | 例:IG | 例:ANOVA p<0.01,η²=0.42;忠实性领先 X% |
| 小模型(VGG16)+ 医学影像 | 例:RISE | 例:稳定性维度显著更优,详见报告 §5.2 |
| 追求效率、在线场景 | 例:Saliency/Grad-CAM | 例:效率快 N 倍,忠实性损失 <X% |
| …… | …… | 每行必须有统计依据 |

配合选做实验 7 的决策树图,形成"一表 + 一树"的最终选择指南。

---

*本文件由全组共同维护;任何技术决策变更需在例会通过后更新本文件并同步全组。*
