# XAI01-04 归因方法模式发现 —— 小组工作指导文件

> 适用组:本项目(XAI01-04)小组(5人) · 更新时间:2026-09-16（按知识工程项目协作方式重新分工）
> 用途:全组统一的角色分工、技术方案、实验设计与协作规范。所有决策以本文件为基准,变更须在例会中同步并更新本文件。

---

## 0. 任务一句话定位

**我们不提出新方法。** 我们做的是:把多类标准归因方法(梯度类/扰动类/博弈论类)放进一个
"方法 × 模型 × 数据集 × 指标"的矩阵里系统比较,用统计方法(ANOVA/Pareto/相关性)发现规律,
最终回答"什么条件下用什么方法最优",产出一份**方法选择指南**。

核心挑战(评分看点)有两处:**实验设计的完备性**(矩阵怎么搭、组合爆炸怎么管)和
**模式发现的统计显著性**(p 值、效应量,不能只看均值排名)。

---

## 1. 任务要求速览(摘自任务书 XAI01-04)

### 1.1 研究问题

| 编号 | 问题 |
|------|------|
| RQ1 | 在"方法×模型×数据集×指标"空间中,是否存在稳定的优势模式——某种方法在某种条件下系统性优于其他方法? |
| RQ2 | 不同归因方法在忠实性、稳定性、效率三个维度上的权衡关系如何——是否存在 Pareto 最优方法? |
| RQ3 | 不同评价指标之间是否存在系统性冲突——高忠实性的方法是否必然低稳定性? |

### 1.2 必做实验

1. **方法×模型交叉对比**(→RQ1):固定数据集和指标,所有方法在 ≥2 种模型上对比,检验交互效应。
2. **Pareto 前沿分析**(→RQ2):在忠实性/稳定性/效率三维空间画各方法 Pareto 前沿。
3. **指标冲突分析**(→RQ3):指标间相关性矩阵,找出冲突最严重的指标对。
4. **模式统计检验**:ANOVA 检验方法/模型/数据集的主效应与交互效应显著性,报告 p 值和效应大小。

### 1.3 选做实验(至多选 2 项,建议选:方法选择决策树 + 方法聚类)

- 实验5:前沿方法(FourierShap / MA-GIG)加入比较
- 实验6:方法聚类(指标空间中发现方法群体)
- 实验7:方法选择决策树(可操作的选择指南)

### 1.4 里程碑(M1–M5,与任务书对齐)

| 里程碑 | 内容 | 关键交付物 |
|--------|------|-----------|
| M1 问题分析 | 实验矩阵设计、组合爆炸管理策略、RQ1–RQ3 拆解 | 矩阵设计文档 |
| M2 初步结果 | 2 种基线方法运行、分析 pipeline 建立 | 可跑通的 pipeline |
| M3 中期结果 | ≥4 种方法运行、初步对比和 Pareto 分析 | 中期检查演示材料 |
| M4 完整实验 | 完整矩阵、ANOVA 和相关性分析、选做实验 | 全量结果 + 统计结论 |
| M5 最终成果 | 报告(Pareto 分析、RQ1–RQ3 回答、方法选择指南)、可复现代码仓库、答辩 PPT | 三个终稿 |

### 1.5 任务书建议的实验空间(我们在此基础上定稿)

- **方法(≥4)**:Grad-CAM / IG / SHAP / RISE / LIME / Ablation(选做:Saliency 作为低成本第 7 种对照)
- **模型(≥2)**:VGG-16 / ResNet-50 / DenseNet-121
- **数据集(≥2)**:ImageNet / VOC / CHNCXR
- **指标(≥3)**:忠实性 / 稳定性 / 效率

---

## 2. 团队协作原则

本项目取消“数据负责人、模型负责人、实验负责人、分析负责人、文档负责人”的永久岗位。所有成员都可以参与数据、模型、归因、跑批、统计、文档、Demo 和复现；每项具体任务只临时指定一名主执行人和至少一名协作人，任务验收后即解除该分工。

当前只记录两项个人状态：

- A `hycx233` 更倾向实验，优先参与归因方法、指标、实验配置、GPU 跑批和失败重跑，但实验不是 A 的专属工作。
- C `oioioi-521` 已完成前期数据收集和资源整理。数据作为全组共享基线，C 后续进入共享任务池，不再被固定为数据人员。

B `YaoYang-123456`、D `lanshuheng9-oss`、E `onlysonder` 不设置固定方向；五人均根据当前瓶颈和意愿动态认领工作。完整协作图、任务池和认领规则见 [`docs/TEAM_AND_ROADMAP.md`](docs/TEAM_AND_ROADMAP.md)，若其他章节仍出现“负责人”字样，仅表示该类工作本身，不代表永久绑定某位成员。

---

## 3. 动态任务认领与验收

1. 例会维护共享任务池，任务卡只包含：任务、主执行人、协作人、验收物、截止时间。
2. A 对实验任务有优先选择权；正式矩阵跑批由全组按设备、时间和当前负载共同分担。
3. C 已完成的数据收集作为既有贡献记录；后续只有在数据缺失、版本或质量问题出现时才补充数据。
4. 每项任务至少由一名未主导该任务的成员复核；任何成员都应能沿完整链路理解上下游。
5. 文档和汇报由参与对应工作的成员同步提供材料，最终统一校对，不设置一名成员包办。

当前共享任务包括：固定数据基线、三模型接口、归因方法、评价指标、36 单元实验矩阵、统计检验、方法选择指南、README、报告、PPT、Demo 和干净环境复现。技术实现与验收要求仍按后续章节执行，但不再与成员永久绑定。

---

## 4. 技术方案(全组统一)

### 4.1 环境与技术栈

- Python 3.10+,PyTorch(建议 2.x,CUDA 版),torchvision
- 归因:`captum`(IG/Saliency/Occlusion/KernelSHAP/DeepLIFT)、`pytorch-grad-cam`、`lime`
- 评价:`quantus`(忠实性/稳定性指标,注意锁版本,API 变动较多)
- 统计:`pandas` / `numpy` / `scipy` / `statsmodels`(ANOVA)/ `scikit-learn`(聚类、决策树)、
  可选 `scikit-posthocs`(Nemenyi)、`autorank`(CD 图)
- 可视化:`matplotlib` / `seaborn`
- 配置:`pyyaml`;进度条:`tqdm`

```bash
# 环境初始化(conda 或 venv 均可,由本任务认领人统一)
conda create -n xai01-04 python=3.10
conda activate xai01-04
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
pip install captum quantus pytorch-grad-cam lime pandas scipy statsmodels scikit-learn matplotlib seaborn pyyaml tqdm
pip freeze > requirements.txt   # 锁版本后全组同步
```

### 4.2 归因方法集(主体 6 + 对照 1)

| 类别 | 方法 | 实现途径 | 说明 |
|------|------|---------|------|
| 梯度类 | Integrated Gradients (IG) | captum | 基线统一用零基线(组内一致即可) |
| 梯度类 | Grad-CAM | pytorch-grad-cam | 注意用 VGG 时目标层选最后一个 conv |
| 梯度类 | Saliency(对照) | captum | 原始梯度,实现成本≈0,用作组内 sanity |
| 扰动类 | RISE | 手写 ~30 行 | 随机掩码平均,N 掩码数记入配置 |
| 扰动类 | LIME | lime 包 | 超像素分割用 quickshift,记入配置 |
| 扰动类 | Occlusion/Ablation | captum | 窗口大小记入配置 |
| 博弈论类 | KernelSHAP | captum | 若太慢可用 DeepSHAP 替代并注明 |

选做(FourierShap / MA-GIG):只有主体 6 种全部跑完且有富余人力时才启动,不影响主线。

**组内约定**:所有方法归因目标统一为**真值类别**(target = ground-truth class),而非预测类别;
认领模型评测任务的成员额外记录预测类别,方便后续"预测正确/错误"子条件分析。

### 4.3 数据集与模型矩阵

| 维度 | 选项 | 备注 |
|------|------|------|
| 数据集 | **ImageNet、VOC(主体 2 个,满足任务书 ≥2)** | 每集取固定评估集 300–500 张分层采样;**CHNCXR 不纳入主线**(2026-09-06 定稿:教师已提供官方完整集下载途径,如需启用再启动,见 data/README.md),"自然图像→医学影像"迁移性分析随之取消 |
| 模型 | VGG16 / ResNet50 / DenseNet121 | torchvision 预训练,统一 224×224 输入 |

---

## 5. 实验矩阵与组合爆炸管理(任务书核心加分点)

### 5.1 矩阵

**主体单元 = 6 方法 × 3 模型 × 2 数据集 = 36 个单元**(2026-09-06 定稿:CHNCXR 不纳入,矩阵为两数据集),每个单元对固定评估集全量计算
忠实性/稳定性/效率全部指标(指标不算乘法项)。逐图结果 + 单元聚合两层保存。

### 5.2 组合爆炸管理策略(写入 M1 文档)

1. **统一评估集**:采样一次,36 个单元共用 → 保证可比,也保证总量可控。
2. **配置驱动 + 断点续跑**:每个单元一个 YAML,中断后从已完成 image_id 续跑。
3. **两阶段**:
   - 开发期(M2):debug 集 30–50 张,快速打通 pipeline、检查各方法报错和指标合理性;
   - 正式期(M3–M4):全评估集,GPU 空闲时间批量跑,记录每个单元耗时。
4. **预算估算**(写进 M1 文档):扰动类指标每图需 ~50 次前向,单 GPU 前向约 30ms →
   500 图 × 50 步 × 30ms ≈ 12 分钟/单元,36 单元 ≈ 7 小时量级,可接受;
   KernelSHAP/LIME 最慢,必要时限制其评估集子集(如 150 张)并在报告中注明(效率本身就是发现点,不要隐瞒)。
5. **不扩维**:主矩阵固定 3 个维度。"预测正确/错误""模型准确率"等条件作为**事后分组变量**分析,不放进主矩阵。

### 5.3 结果统一 schema(§7.3 细述)

逐图:`image_id, dataset, model, method, metric, value, time_ms`
单元:`method, model, dataset, metric, mean, std, n, config_hash`

---

## 6. 里程碑计划(周历)

> 按 2026-09-04(第 1 周)起算;**以指导教师公布的 deadline 为准调整**,但"M3 前必须有完整小规模端到端结果"是硬底线。

| 周次(日期) | 里程碑 | 各角色重点 | 组会交付 |
|-----------|--------|-----------|---------|
| W1 (09-04~09-11) | **M1 问题分析** | 全组共同确定矩阵和协作方式；C 完成前期数据收集 | 实验矩阵定稿、数据基线、风险清单 |
| W2 (09-12~09-18) | **M2 初步结果** | 动态认领模型接口、Grad-CAM/IG、指标和聚合脚本；A 优先实验任务 | 首批逐图 CSV、pipeline 演示 |
| W3 (09-19~09-25) | M2→M3 | 全组按当前瓶颈接入其余方法、联调并复核 debug 结果 | 全方法 debug 集结果汇总表 |
| W4 (09-26~10-02) | **M3 中期结果** | 并行启动正式跑批、初步统计、报告同步和 Demo 整合 | 中期检查演示材料(PPT 初稿) |
| W5–W6 (10-03~10-16) | **M4 完整实验** | 按设备和时间拆分 36 单元；跑批、日志复核和统计分析同步进行 | 全量长表 + 统计检验初版结论 |
| W7 (10-17~10-23) | M4 收尾 | 动态认领补漏、异常诊断、决策树/聚类和选择指南 | 方法选择指南初稿 |
| W8–W9 (10-24~11-06) | **M5 最终成果** | 全组交叉复现、校对本人参与部分并共同完成报告、PPT 和 Demo | 报告/PPT/代码仓库三件套 |
| W10 (11-07~) | 缓冲 + 答辩演练 | 全组 | 答辩演练、常见问题清单 |

**例会节奏**:每周 1 次固定例会(30–60 分钟，主持人与看板维护人可轮换，纪要入 `docs/meetings/`)，
会前每人 3 句话(做了什么/卡在哪/想认领什么);会上只分配当前任务，不形成永久角色。

---

## 7. 协作与工程规范

### 7.1 仓库目录(对齐任务书"代码仓库要求")

```
.
├── data/             # 数据集原始文件 + README(全组共享，C 已完成初始收集)
├── preprocessing/    # 数据加载、评估集采样、metadata 生成
├── models/           # 模型加载封装、微调代码、checkpoint
├── experiments/      # 归因方法、评价指标、run_unit.py、configs/
├── results/          # 逐图 CSV + 单元聚合 CSV + 运行日志
├── analysis/         # 统计脚本、图表、方法选择指南
├── docs/             # 报告、PPT、会议纪要、里程碑材料
├── requirements.txt
└── README.md         # 环境配置 + 运行说明(全组共同维护)
```

### 7.2 Git 规范

- 一条硬规则:**`results/` 与 `data/` 不进 git 仓库**(大文件放网盘/共享盘,git 里只放生成它们的脚本与 schema 说明)。
- 分支使用 `feat/<topic>`、`research/<topic>`、`fix/<topic>`、`docs/<topic>`；`main` 始终保持可复现、可说明。
- 每个 PR 至少由一名非作者成员审阅；涉及跨模块接口时，验收搭档必须参与 review。
- commit 信息使用 Conventional Commits（如 `feat: add unified attribution interface`、`docs: update M2 demo`）；每个里程碑打 tag(`m1`、`m2`…)。
- PR 必须写清运行命令、验证结果、数据/配置版本和已知限制；AI 辅助内容由提交者理解并负责。

### 7.3 结果文件 schema(全组对齐,改了先开例会)

`results/per_image.csv`:

| 列 | 示例 |
|----|------|
| image_id | `n01440764_ILSVRC2012_val_00004102.JPEG` |
| dataset | `imagenet` |
| model | `resnet50` |
| method | `ig` |
| metric | `faithfulness_morf_auc` |
| value | `0.1823` |
| time_ms | `243.1`(仅 efficiency 类指标填) |

`results/units.csv`:`method, model, dataset, metric, mean, std, n, config_hash`(config_hash 指向 YAML 快照)。

### 7.4 最终报告结构(建议,对齐任务书要求)

1. 问题分析(背景、RQ1–RQ3 拆解)
2. 方法设计(实验矩阵、方法/模型/数据集/指标说明、组合爆炸管理策略)
3. 实验结果(方法×模型交叉对比、Pareto 前沿、指标冲突)
4. 统计检验(ANOVA、效应量、交互效应;RQ1–RQ3 的明确回答)
5. 方法选择指南(决策树/规则表,见附录 C)
6. 错误分析与局限(失败的实验、指标局限、计算资源约束)
7. 结论与改进方向

### 7.5 学术规范

- 各方法与指标必须引用原始论文(IG: Sundararajan et al. 2017;SHAP: Lundberg & Lee 2017;
  RISE: Petsiuk et al. 2018;LIME: Ribeiro et al. 2016;Grad-CAM: Selvaraju et al. 2017;等)。
- 若同班其他组做 XAI01-01~03,可以交流方法与踩坑经验,但**代码与报告必须独立完成**。
- 所有结论必须附 p 值/效应量,报告里"显著"一词要有统计依据。

---

## 8. 风险与应对

| # | 风险 | 应对 |
|---|------|------|
| 1 | 课程网盘链接 2026-12-11 过期 | **W1 内下载**完毕,本地 + 网盘双备份 |
| 2 | ImageNet 无法获取 | 已解决:本地 500 张子集 + 教师提供的公开镜像(HF/GitHub/百度网盘,见 resources/xai/imagenet-1k.md);报告需如实说明子集来源与筛选标准 |
| 3 | 算力不足/训练慢 | 评估集 500→300 可缩;扰动步数 50→30;KernelSHAP 限子集;实验尽量夜间批量跑 |
| 4 | Quantus/captum API 与文档不符 | requirements.txt 锁版本;首次运行先在 debug 集验证每个指标数值范围是否合理(如 AUC∈[0,1]) |
| 5 | (已移除)CHNCXR 上预训练模型准确率过低 | CHNCXR 不纳入主线,此风险随矩阵缩减取消;若后续启用,按微调决策点处理 |
| 6 | 某组员进度落后 | 看板周更 + 例会点名;Owner 职责不清的任务,组长直接分派;M4 起全组支援实验/文档 |
| 7 | 结论不显著(没有"稳定优势模式") | 这本身就是合法发现——如实报告"在哪些条件下无差异",并给出效应量;决策树/指南退化为"不推荐某方法的条件",同样有价值 |

---

## 9. 交付物检查清单(对照任务书)

**M1**:实验矩阵设计文档、组合爆炸管理策略、RQ1–RQ3 拆解 ✓
**M2**:2 种基线方法运行、分析 pipeline 建立、数据预处理代码 ✓
**M3**:≥4 种方法运行、初步对比和 Pareto 分析、中期检查演示材料 ✓
**M4**:完整实验矩阵、ANOVA 和相关性分析、选做实验结果 ✓
**M5**:完整报告(模式发现 + Pareto 分析 + RQ1–RQ3 回答 + 方法选择指南)、可复现代码仓库、答辩 PPT ✓
**代码仓库**:目录结构 `data/ preprocessing/ models/ experiments/ results/` ✓
关键实验命令行可复现 ✓ README 含环境配置与运行说明 ✓

---

## 附录 A:本周(W1)行动清单

- [x] 取消永久岗位制，改为共享任务池与动态认领，见 §2–§3
- [x] C `oioioi-521`:完成数据收集、网盘资源备份、CHNCXR 盘点和 ImageNet/VOC 获取确认
- [ ] 动态认领:环境搭建、模型前向、YAML 与 CSV schema
- [ ] 动态认领:RQ1–RQ3 统计假设、README 骨架和固定 Demo
- [ ] 全组:第 1 次例会过一遍本文件,有异议当场改

## 附录 B:常用命令示例

```bash
# 跑一个实验单元
python experiments/run_unit.py --config configs/ig_resnet50_imagenet.yaml

# 聚合逐图结果
python analysis/aggregate.py --input results/per_image.csv --output analysis/data_long.csv

# 三因素 ANOVA
python analysis/anova.py --metric faithfulness_morf_auc

# Pareto 前沿图
python analysis/pareto.py --dims faithfulness,stability,efficiency
```

## 附录 C:方法选择指南模板(M5 成果样式)

| 条件 | 推荐方法 | 依据(证据) |
|------|---------|-----------|
| 大模型(ResNet50)+ 自然图像 | 例:IG | 例:ANOVA p<0.01,η²=0.42;忠实性领先 X% |
| 小模型(VGG16)+ 医学影像 | 例:RISE | 例:稳定性维度显著更优,详见报告 §5.2 |
| 追求效率、在线场景 | 例:Saliency/Grad-CAM | 例:效率快 N 倍,忠实性损失 <X% |
| …… | …… | 每行必须有统计依据 |

配合选做实验 7 的决策树图,形成"一表 + 一树"的最终选择指南。

---

*本文件由全组共同维护;任何技术决策变更需在例会通过后更新本文件并同步全组。*
